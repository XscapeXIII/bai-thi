import { createAction } from "@reduxjs/toolkit";

export const addAction = createAction("ADD_LIST");
export const editAction = createAction("EDIT_LIST");
export const removeAction = createAction("REMOVE_LIST");
